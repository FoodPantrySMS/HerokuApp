const request = require('request-promise');

module.exports = {
    callAPI: callAPI
}

function callAPI(zipcode) {
    request("")
    .then(function(data) {

    })
    .catch(function(err) {

    });
}